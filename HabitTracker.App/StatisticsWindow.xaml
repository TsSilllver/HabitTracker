<Window x:Class="HabitTracker.App.StatisticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="Статистика" Height="500" Width="700"
        WindowStartupLocation="CenterOwner" Background="{StaticResource BackgroundBrush}" Foreground="{StaticResource ForegroundBrush}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ComboBox x:Name="HabitsCombo" Margin="5" DisplayMemberPath="Name" SelectionChanged="HabitsCombo_SelectionChanged"
                  Background="#3A3A3A" Foreground="White" Padding="5"/>

        <lvc:CartesianChart Grid.Row="1" x:Name="ProgressChart" LegendLocation="Top" Series="{Binding Series}"/>
    </Grid>
</Window>